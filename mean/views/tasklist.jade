extends layout

block content
    h1.
        Task List
    table
        tr
            th name
            th status
            th priority
            th due date
            th actions
        each task in tasklist
            tr
                td #{task.taskDescription}
                td #{task.taskStatus}
                td #{task.priority}
                td #{task.taskDue}
                td
                    a(href="tasklist/#{task._id}") View
                    span  | 
                    a(href="updatetask/#{task._id}") Edit
                    span  | 
                    a(href="deletetask/#{task._id}") Delete